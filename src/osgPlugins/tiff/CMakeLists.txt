INCLUDE_DIRECTORIES( ${TIFF_INCLUDE_DIR} )

SET(TARGET_SRC ReaderWriterTIFF.cpp )

SET(TARGET_LIBRARIES_VARS TIFF_LIBRARY )

#### end var setup  ###
SETUP_PLUGIN(tiff)

target_include_directories(${TARGET_TARGETNAME} PRIVATE ${TIFF_INCLUDE_DIRS})
target_link_libraries(${TARGET_TARGETNAME} ${TIFF_LIBRARIES})
